{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "files=$( { git diff --name-only --diff-filter=ACM -- \"*.py\"; git ls-files --others --exclude-standard -- \"*.py\"; } | sort -u ); [ -z \"$files\" ] && exit 0; out=$(uv run pre-commit run --files $files 2>&1); code=$?; [ $code -eq 0 ] && exit 0; printf \"%s\\n\" \"$out\"; exit 2",
            "timeout": 120,
            "statusMessage": "Running pre-commit checks…"
          }
        ]
      }
    ]
  }
}
